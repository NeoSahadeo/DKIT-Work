# Create a file link to sample_amanda
file = open("sample_amanda", "w")

# Write this string to the file using the .write method
file.write("Hi, Amanda's name is Amanda")

# Close the file link
file.close()
